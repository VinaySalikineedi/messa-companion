<?xml version="1.0" encoding="utf-8"?>
<!--
    Messa Companion APK manifest (open-source-phone.md section 2/3/4).

    Deliberately requests ZERO listening/server permissions: this app
    never opens a port and is never reachable from the network (section
    3.1 "Zero inbound ports on the phone") - every permission below
    exists only to (a) dial OUT over a WebSocket, (b) stay alive as a
    foreground service against Android's battery/Doze restrictions
    (section 4, roadblock #3/#5), and (c) show the touch killswitch
    overlay (section 3.4).
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <!-- Touch killswitch overlay (section 3.4): a transparent full-screen
         overlay that detects a genuine user touch while Messa is driving
         the phone and immediately signals abort. SYSTEM_ALERT_WINDOW is
         the one permission here that needs an explicit user grant via
         Settings (requested from MainActivity, never silently assumed). -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.MessaCompanion"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".BridgeWatchdogReceiver"
            android:enabled="true"
            android:exported="false" />

        <!--
            Android 14+ (API 34) requires explicit foregroundServiceType.
            "connectedDevice|dataSync" accurately describes bridging the phone's
            local ADB daemon with the remote outbound WebSocket data stream,
            preventing OS-level background termination during screen-off sleep.
        -->
        <service
            android:name=".BridgeForegroundService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice|dataSync" />

        <!--
            Native Android Accessibility Service providing persistent 24/7 automation,
            gesture execution, screen inspection, and screenshot capture.
        -->
        <service
            android:name=".MessaAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>

</manifest>
