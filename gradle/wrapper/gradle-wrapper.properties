distributionBase=GRADLE_USER_HOME
distributionPath=wrapper/dists
distributionUrl=https\://services.gradle.org/distributions/gradle-8.7-bin.zip
zipStoreBase=GRADLE_USER_HOME
zipStorePath=wrapper/dists
# Note: the gradle-wrapper.jar binary itself is intentionally not
# committed by this sandbox (no Android SDK/Gradle-wrapper generation
# tooling is available in this build environment -- see companion-apk/
# README.md). Opening this project in Android Studio, or running
# `gradle wrapper` once from a machine with a real Gradle install, will
# regenerate gradlew/gradlew.bat/gradle-wrapper.jar from this file.
