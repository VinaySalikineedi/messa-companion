// Top-level build file: plugin versions are declared here (via the
// `plugins` block with `apply false`) and actually applied in
// app/build.gradle.kts -- standard Gradle Kotlin DSL multi-module shape.
plugins {
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
